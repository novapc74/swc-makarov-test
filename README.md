# 🚀 SWC test task.

## ✨ Ключевые особенности
- **Auth**: Регистрация и вход через Laravel Sanctum (Bearer токены).
- **Security**: Каждый пользователь видит и управляет только своими задачами.
- **RESTful**: Чистые JSON-ответы, пагинация, заголовки `Location`, кастомная обработка ошибок (401, 403, 404, 422).
- **Queues**: Уведомления о создании задач уходят в фоновую очередь (Redis + Mailpit).
- **Cron**: Автоматическая проверка дедлайнов. Если задача просрочена — летит Email.
- **Tests**: Полное покрытие Feature и Unit тестами.
---
## 🛠 Быстрый старт
### Requirements: Docker + Makefile
Для развертывания проекта вам нужен только **Docker**. Все команды выполняются через `make`.
### 1. Установка проекта
Клонируйте репозиторий и запустите установку. Команда сама создаст `.env`, установит зависимости, поднимет контейнеры и наполнит базу данными.
```bash
mkdir swc_test_makarov
cd swc_test_makarov
git clone git@github.com:novapc74/swc-makarov-test.git ./
make install
```
### 2. Запуск сервисов
   Если проект уже установлен, просто поднимите Docker:
```shell
make up
```
_Интерфейс почты доступен по адресу: http://localhost:8025_

### 📅 Планировщик и Очереди
Проверка просроченных задач
Система автоматически проверяет due_date. Для ручного запуска проверки выполните:
```shell
make check-overdue
```
_Логика: Если задача просрочена и уведомление еще не отправлялось (is_overdue_notified = false), пользователь получит письмо, а флаг в БД обновится._
### 🧪 Тестирование
Запуск всех тестов (Auth, CRUD, Validation, Unit):
```shell
make test
```
_Тесты используют изолированную базу данных testing в PostgreSQL._

⚙️ Полезные команды Makefile
- make seed — полная очистка БД и повторное наполнение.
- make shell — вход в консоль PHP-контейнера.
- make down — остановка и удаление контейнеров.
- make schedule - запуск планировщика задач

